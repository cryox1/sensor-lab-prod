# <Area name>

**Status:** As-built <!-- overall file status is implicit; requirements carry their own -->
**Scope:** <what this spec covers — page/route/service, and what it does not>

## Behavior (as-built)

<Prose + bullets describing what the area does today: data sources (endpoints,
polling intervals, WebSocket), UI structure, interactions, edge cases worth
knowing. Reference source files like `web/app/foo/page.jsx` and API routes
like `GET /devices`. This section is updated in the same commit as any
behavior change.>

## Requirements

<Numbered requirements for features beyond the original as-built state. Each
starts life as `Proposed` and flips to `Implemented` in the commit that ships
it. Delete nothing; superseded requirements get status `Superseded (by Rn)`.>

### R1 — <short feature name> (Proposed | Implemented)

<One-paragraph intent.>

Acceptance criteria:

- **Given** <precondition>, **when** <action>, **then** <observable outcome>.
- …

## Non-goals

- <Explicitly out of scope, so nobody "helpfully" adds it.>

## Open questions

- <Unresolved decisions, or "none".>
